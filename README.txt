Simple Arduino TMP102 program to prove out the part, especially in extended mode and supporting the alert limit feature. I could find neither of these in existing TMP102 libraries.

Convert this to a proper Arduino library once it has been shown to work correctly